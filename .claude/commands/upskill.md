# /upskill - Skill-gap heatmap and learning plan

You are running the upskill skill. **Do not fork this spec.** Follow `.claude/skills/upskill/SKILL.md` exactly (aggregate tracker mode, targeted URL mode, `gaps` weighting from ranked `seen_jobs.json` entries).

`$ARGUMENTS` may be empty (all tracked jobs) or a posting URL.
